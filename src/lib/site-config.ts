// Static branding used in browser-tab <title> meta (route head()). The live,
// on-page profile (sidebar/home/contact) is admin-editable via site_settings in
// the DB; this constant only powers SEO titles, so change it here if your name
// changes.
export const SITE_NAME = "Omansh Kothari";
